# Optional local overrides — not required for production clients.
# Production wallet defaults are baked into Jiggle-V3 (no env required):
#   EXPO_PUBLIC_APP_STATUS_URL=https://jiggle-status.expo.app/status
#   EXPO_PUBLIC_APP_STATUS_FALLBACK_URL=https://raw.githubusercontent.com/Jiggle-Wallet/jiggle-app-status/main/status.json
#
# Deploy (CI):
#   EXPO_TOKEN is set in GitHub Actions secrets for `eas deploy --prod`

# EXPO_TOKEN=
